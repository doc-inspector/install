# DocInspector Pro: Permanent Environment Paths & System Architecture

This document serves as the authoritative memory record for all project paths, build directories, and environment locations across desktop application builds and website deployments.

---

## 1. Desktop Application Paths (`DocInspector_legacy`)

- **Root Desktop Program Path**: `D:\APP 2.2.1 REAL TIME WORK\DocInspector_legacy`
  - Main directory containing the C# project, Inno Setup script (`DocInspector.iss`), assets, dependencies, and build configurations.

- **C# Project Source Code**: `D:\APP 2.2.1 REAL TIME WORK\DocInspector_legacy\DocInspectorCSharp`
  - Active C# WPF source code directory containing all `.xaml`, `.xaml.cs`, engine classes (`SanitizeEngine.cs`, `BorderouEngine.cs`, `RedactionEngine.cs`), and `Lang/*.psd1` localization files.

- **Release Build & Inno Setup Source Payload**: `D:\APP 2.2.1 REAL TIME WORK\DocInspector_legacy\Release_DocInspectorPro`
  - The staged release directory updated after compiling the C# project. Inno Setup reads `Release_DocInspectorPro\*` to bundle all executable files, DLLs, dependencies, and bundled tools (`Tools/`).

- **Compiled Installer Executable**: `D:\APP 2.2.1 REAL TIME WORK\DocInspector_legacy\CompiledSetup`
  - Contains the final output installer executable `DocInspector_Setup.exe` (~250MB) generated by Inno Setup.

---

## 2. Website & VPS Infrastructure Paths

- **Local Website Repository & VPS Backup**: `C:\Users\tonuv\OneDrive\Desktop\Public2`
  - Local repository containing all HTML pages (`Public/`), blog generator scripts (`build-blog.py`), SEO manager (`seo-manager.py`), sitemap, assets (`Public/assets/`), and GitHub deployment configuration.

- **GitHub Repository**: `https://github.com/doc-inspector/install.git` (`main` branch)
  - Remote repository configured with GitHub authentication.

- **Live Hetzner VPS Server**:
  - Remote production VPS hosting the online SaaS API (`server.py`), Ghostscript, LibreOffice, OCR engines, and tools processing online document requests.
